// This is a TEMPLATE, not the real config. The actual js/config.js is
// generated automatically by .github/workflows/deploy.yml at deploy time,
// using your OPENROUTER_API_KEY repository secret — it is never committed.
//
// For local testing you can copy this file to js/config.js and fill in a
// key by hand, but don't commit that copy (js/config.js is gitignored).
window.OPENROUTER_INJECTED_KEY = "";
